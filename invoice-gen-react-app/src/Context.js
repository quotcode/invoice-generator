import {createContext} from "react";

// Exporting the AppContext so that wherever we require the data we can just import AppContext and just perform our actions
export const AppContext = createContext();